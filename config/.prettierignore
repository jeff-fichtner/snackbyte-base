node_modules/
dist/
coverage/
package-lock.json
*.tsbuildinfo

# Generated by the build (scripts/build.mjs writes the baked environment identity here);
# its content varies per build target, so it is not part of the format gate.
# The `**/` prefix is load-bearing: a slash-containing pattern is anchored to the ignore
# file's own directory, so a bare `src/env.generated.ts` here would mean `config/src/...`
# and silently match nothing.
**/src/env.generated.ts

# Spec-workflow scaffolding — not shipped code, not formatted by the app gate
.specify/
.claude/
specs/
CLAUDE.md
